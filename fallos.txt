en la funcion ft_last_word se esta volviendo a dar memoria a una variable que ya tiene??

las redirecciones de entrada (las dobles tambien) pueden ser:
cat < infile
cat <infile
< infile cat
<infile cat
redirecciones y builtin juntos no funciona

echo con pipes no imprime las comillas, hay que cambiar los builtin para que utilice los tokens

si ejecuto cat Makefile | wc -c > outfile sale "minishell $ exit"